import styled from '@emotion/styled';

export const TabStyled = styled('div')`
  svg {
    width: 26px;
    height: 26px;
    display: inline;
    margin-right: 20px;
  }
  
  .btn-wrapper {
    display: flex;
    justify-content: flex-end;
    gap: 8px;
  }
`;
