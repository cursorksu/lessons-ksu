import styled from '@emotion/styled'

export const StudentProfileStyled = styled.div`
	position: relative;
	.ksu-modal-trigger {
		position: absolute;
		top: 20px;
		right: 50px;
		z-index: 10;
	}
`;
